---
title: "Physician Strategy"
author: "Tim H"
date: today
format: 
  html:
    theme: style/hsiaR_html_style.scss
    toc: true
    # fig-width: 8
    # fig-height: 5.5
    # fig-align: center
    # fig-dpi: 150
    # dev: svg  
    lightbox: 
      match: auto
      effect: fade
      desc-position: bottom
      loop: false
    # include-after-body: _fullscreen-button.html
bibliography: 
  - references.bib
editor_options: 
  chunk_output_type: console
execute: 
  echo: false
  warning: false
  message: false
params:
  ggplot: yes
---

```{r setup}
library(targets)
invisible(extrafont::choose_font("BC Sans", quiet = T))
extrafont::loadfonts()

ggplot2::theme_set(ggthemes::theme_clean(base_family = "BC Sans"))
echarts4r::e_common(font_family = "BC Sans", theme = "Westeros")
```

## Physicians in BC

```{r}
if (params$ggplot) tar_read(gg1) else tar_read(e1)
```

## Physicians Comparison

```{r}
if (params$ggplot) tar_read(gg2) else tar_read(e2)
```

## Physician Age

```{r}
#| column: page
#| fig-width: 10

if (params$ggplot) tar_read(gg3) else tar_read(e3)
```

## Gender

```{r}
if (params$ggplot) tar_read(gg4) else tar_read(e4)
```
